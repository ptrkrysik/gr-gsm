sudo: required

language: ruby

env: 
  - DOCKERFILE=DebianJessie.docker IMGNAME=debjess-grgsm
  - DOCKERFILE=Ubuntu14.docker IMGNAME=ubu14-grgsm
  - DOCKERFILE=Ubuntu15.docker IMGNAME=ubu15-grgsm

services:
  - docker

before_install:
  - cat $DOCKERFILE > Dockerfile ; docker build -t $IMGNAME .

script:
  - docker run -it --rm $IMGNAME /src/build_test_scripts/decode.sh
